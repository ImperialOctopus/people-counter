import 'package:flutter/material.dart';

class InvalidStateComponent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Invalid state. Please restart.'));
  }
}
