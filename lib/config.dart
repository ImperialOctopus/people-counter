const String appTitle = 'People Counter';

// If false, displays a room code text box to enter the room.
const bool presetRoom = false;
// Preset room to join if presetRoom is true.
const String presetName = 'tring_together';
