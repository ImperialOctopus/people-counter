/// Title of app.
const String appTitle = 'Tring Together Counter';

/// Path of header image.
const String headerLocation = 'carnival_logo.webp';

/// Hides option to enter room code.
const bool allowCustomRoomCode = false;

/// Preset room to join if presetRoom is true.
const String roomCode = 't_carnival_2022';

/// Show option on menu to load the stats page.
const bool allowStats = false;

/// Show reset button.
const bool allowReset = true;
